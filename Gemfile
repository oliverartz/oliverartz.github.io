source "https://rubygems.org"

# Match the GitHub Pages build environment.
gem "github-pages", group: :jekyll_plugins

# Performance optimization (Windows only).
gem "wdm", "~> 0.1.0" if Gem.win_platform?
